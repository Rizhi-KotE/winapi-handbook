package common.service;

import model.WinApiParameter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WinApiParameterRepository extends JpaRepository<WinApiParameter, Long>{
}
